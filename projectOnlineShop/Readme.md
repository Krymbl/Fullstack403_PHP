# Проект - Онлайн магазин товаров

## 1 этап: Каркас приложения с контроллерами

*Домашнее задание (lab06)*

Требования
* Структура проекта:
    * папка public со всей статикой, включая точку входа index.php, внутри подпапки для css, js и изображений
    * папка src для всего кода
    * файл autoload.php в корне проекта, в котором должны быть описаны все правила автозагрузки (по PSR-4)
* Внутри src
    * отдельные классы для роутера, описывающего маршрутизацию (сами маршруты нужно вынести в отдельный файл: либо статический конфиг, обрабатываемый в index, либо в routes.php, который будет сам передавать роутеру маршруты) и базовый абстрактный контроллер, от которого будут наследоваться остальные контроллеры
    * папка со всеми контроллерами, каждый адрес должен обрабатывать отдельный класс, допустимо обрабатывать логически связанные страницы разными методами одного контроллера
* Весь общий функционал контроллеров должен быть вынесен в абстрактный контроллер, весь функционал запросов (извлечение параметров, определение http метода и т.д.) - в роутер.


# Lab06 — Каркас приложения с контроллерами

## Структура проекта

```
project/
│
├── logs/                               # Файлы логов 
│   └── app.log                         # Основные логи приложения
│
├── public/                             # Статика и точка входа
│   ├── index.php                       # Точка входа — подключает autoload и routes
│   └──                                 # Статика
│       ├── css/                        # Стили
│       ├── js/                         # Скрипты
│       └── images/                     # Изображения
│
├── src/                                # Весь исходный код
│   ├── Attributes/
│   │ └── Route.php                     # Атрибут для маршрутизации
│   │
│   ├── Core/                           # Ядро приложения
│   │   ├── Router.php                  # Маршрутизация, HTTP-методы, параметры запроса
│   │   ├── AbstractController.php      # Базовый абстрактный контроллер
│   │   ├── Database.php                # Синглтон PDO
│   │   ├── Config.php                  # Загрузка .env через vlucas/phpdotenv
│   │   └── Loggers/
│   │       ├── AbstractLoggerFactory.php
│   │       └── AppLoggerFactory.php
│   │
│   ├── Controllers/                    # Конкретные контроллеры
│   │   │
│   │   ├── Admin/                      # Контроллеры админ-панели
│   │   │   ├── CatalogController.php   # Просмотр всех товаров
│   │   │   ├── AddController.php       # Добавление товара
│   │   │   └── EditController.php      # Редактирование / удаление товара
│   │   │
│   │   ├── User/                       # Контроллеры пользовательской части
│   │   │    ├── AuthController.php     # Вход, регистрация, выход
│   │   │    ├── HomeController.php     # Главная — акции, баннер, хиты
│   │   │    ├── CatalogController.php  # Каталог с фильтрацией и пагинацией
│   │   │    ├── ProductController.php  # Страничка одного товара
│   │   │    ├── CartController.php     # Корзина
│   │   │    ├── OrderController.php    # Оформление заказа + успех / ошибка
│   │   │    └── ProfileController.php  # Личный кабинет + история заказов
│   │   │
│   │   └── ErrorController.php         # Обработка ошибок 404, 403
│   │   
│   ├── Dto/
│   │   └── UserDto.php                 # Облегчённый объект пользователя
│   │
│   ├── Models/                         # Модели — сущности базы данных
│   │   ├── User.php                    # Пользователь, роль, сессия
│   │   ├── Product.php                 # Товар, количество, доступность
│   │   ├── Cart.php                    # Корзина, связь с пользователем
│   │   ├── Order.php                   # Заказ, данные получателя
│   │   ├── OrderItem.php               # Позиция заказа — товар + цена на момент покупки
│   │   ├── Brand.php
│   │   └── Category.php
│   │
│   ├── Enums/                          # Перечисления
│   │   ├── Role.php                    # Роли пользователя (admin, user)
│   │   ├── Status.php                  # Статус заказа
│   │   ├── DeliveryType.php            # Тип доставки (доставка, самовывоз)
│   │   └── PaymentMethod.php           # Способ оплаты (карта, наличные)
│   │
│   └── Repository/
│       ├── Repository.php (interface)
│       ├── BrandRepository.php
│       ├── CartRepository.php
│       ├── CategoryRepository.php
│       ├── OrderRepository.php
│       ├── OrderItemRepository.php
│       ├── ProductRepository.php
│       └── UserRepository.php
│
├── vendor/
├── logs/
├── .env
├── composer.json
└── composer.lock
```

---

## Зоны ответственности

| Файл / Папка                      | Ответственность                                                         |
|-----------------------------------|-------------------------------------------------------------------------|
| `public/index.php`                | Точка входа: подключает `autoload.php` и `routes.php`, запускает роутер |
| `autoload.php`                    | PSR-4 автозагрузка всех классов из `src/`                               |
| `src/Core/Router.php`             | Маршрутизация, определение HTTP-метода, извлечение параметров запроса   |
| `src/Core/AbstractController.php` | Общий функционал всех контроллеров (рендер, редирект и т.д.)            |
| `src/Controllers/Admin/`          | Контроллеры админ-панели                                                |
| `src/Controllers/User/`           | Контроллеры пользовательской части                                      |
| `src/Models/`                     | Модели сущностей базы данных                                            |
| `src/Enums/`                      | Перечисления — роли, статусы, типы доставки и оплаты                    |
| `src/routes.php`                  | Декларация маршрутов, передача их в роутер                              |
| `ErrorController.php`             | Обработка ошибок 404, 403                                               |

---

## 2 этап: Миграция семестрового проекта на Composer, добавление загрузки конфига, логирования и обработки ошибок
*Домашнее задание (lab07)*

Требования:

- Настроить автозагрузку в composer.json
- Использовать любой модуль для работы с конфигом (например, vlucas/phpdotenv), конфиг можно реализовать не в .env, а в любом текстовом формате: json, xml, yaml, ini и т.д.
- Использовать любой модуль для логов (например, monolog/monolog) и реализовать запись всех ошибок (в среде dev или при включенном debug есть смысл писать в лог избыточную информацию). Лог должен содержать время ошибки, степень серьёзности и сообщение
- Реализовать классы исключений - исходя из логики вашего проекта, использовать их везде, где могут возникнуть неожиданные результаты и ошибки
- При критических ошибках реализовать возврат кодов 5** и соответствующих страниц с сообщениями для пользователей

---

## 3 этап: Интеграция базы данных PGSQL в семестровый проект
*Домашнее задание (lab08)*

Требования:

- Перенести все данные, которые хранились в файлах, массивах или сессиях в БД
- Завести классы-модели для всех сущностей по теме вашего проекта (по аналогии с практикой)
- Создать в Core класс Database, реализующий Singleton: инициализирует PDO при первом вызове, далее - возвращает статическим методом тот же экземпляр. Конфигурация подключения - из .env.
- Обязательно все операции БД должны быть обёрнуты в try-catch, ошибки - залогированы
- Если в .env включена отладка (APP_DEBUG) - выводить ошибки в человекочитаемом виде пользователю на страницу

---
## 4 этап: Маршрутизация на атрибутах
*Домашнее задание (lab09)*

Требования:
- Создать атрибут Route (цель - метод, аргументы - путь, метод или массив методов)
- Заменить всю маршрутизацию в проекте на атрибуты над методами контроллеров, переделать класс Router: добавить метод register, принимающий на вход список классов контроллеров и извлекающий из метаданных все маршруты. Список передавать из фронтэнд-контроллера.
